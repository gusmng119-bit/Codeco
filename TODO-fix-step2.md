# TODO-fix (step 2)

## Completed
- Rewrote `src/features/Dashboard/Dashboard.jsx` to remove merge-conflict artifacts.
- Defined `currentPage`, `outletContext`, and ensured pages render via `page` state.

## Next steps
- Rewrite/clean `src/features/Home/Home.jsx` to remove merge-conflict markers and fix `downloadCertificate`.
- Run `npm run dev` (or `npm run build`) to confirm compile.

